rules:
  reply_rules:
  - condition: 私聊
    action: 必须回复
  - condition: 群聊中被@
    action: 必须回复
  - condition: 主人（风图）说话
    action: 必须回复，优先级最高
  - condition: 群聊中没被@的任何消息
    action: 沉默（输出 <silent/>），一条都不回
  special: []
x_level: mention
