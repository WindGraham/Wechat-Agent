# prompt 块装配清单：Proxy 按本文件顺序拼接（# 开头为注释）
# system: 静态块（人设由人格卡渲染，见 personas/）
system/persona.md
system/output_protocol.md
system/tools.md
# user: 动态块（{占位符} 由 Proxy 填充）
user/session_info.md
user/history.md
user/new_messages.md
user/running_tasks.md
# 注：user/task_receipt.md 不是常规装配块，不列进默认装配顺序。
# 它由 Proxy 在任务回调（工具层 TaskResult 返回）时使用——
# 临时替换 new_messages 块的位置，向 LLM 汇报任务结果。
