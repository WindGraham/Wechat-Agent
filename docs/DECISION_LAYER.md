# 决策层（Decision Layer）

> 本项目的 agent 核心。**如何回复用户信息，决策权全部在这一层。**
> 系统里唯一调用 LLM 的层。拼 prompt（上下文工程）也全部在这一层。

## 一、运行循环

```
交互层 MessageEvent（"某某会话有 N 条新消息，@我=…"）
  │
  ▼
EventGate     全局暂停/静默、群类型规则、连续错误熔断（纯规则，不调 LLM）
  │
  ▼
ContextBuilder  拼 prompt（下文输入格式）：
  │   人格卡 + 会话历史（调交互层 get_context，默认 200 条）
  │   + 新消息标注 + 屏幕状态摘要 + 工具说明
  ▼
ReplyEngine   LLM 循环（目标：原生 function calling）：
  │   模型可调用 chat_history 工具再查更早记录（回灌后续生成）
  ▼
OutputParser  解析输出信号（下文输出格式）→ 交交互层 execute()
```

## 二、输入 prompt 格式（本层组装）

```
【人设】（人格卡全文：身份/性格/说话规则/禁区/核心记忆）

【可用工具】
- chat_history(session, keyword?, before?, n): 检索某会话更早的聊天记录
  （需要更多上下文时调用，最多 3 次）

【会话信息】会话: 特高课（群聊） | 时间: 2026-08-08 15:20 周六

【历史记录】（最近 200 条，[我]=你自己，[@我]=@你的消息）
帽子女孩: 这周去不去爬山
[@我] Leisure: @陈曦 你说呢
[我] Leisure: 我都行

【新消息】（本次触发，需要你决策）
Leisure: @陈曦 那就这么定了
```

规则：历史与新消息分区明确；新消息单独成区；多媒体消息在记录里
直接是"图片内容：…"的文本（交互层已标注）；历史默认 200 条，可配置。

## 三、输出信号格式（OutputParser 只认这三种）

```
1. [SILENT]                     → 不回复
2. 纯文本                        → 默认动作 send_text（当前会话）
3. 动作行（首行 JSON，一轮最多 2 个）：
   {"send": "text",  "session": "特高课", "text": "定了，周六见"}
   {"send": "image", "session": "特高课", "url": "https://..."}
   {"send": "file",  "session": "风图",   "path": "/tmp/x.pdf"}
```

设计理由：

- 纯文本默认 = 发文字：最高频动作零格式负担，模型服从率最高
- `session` 显式携带：为"事件来自 A、回复发到 B"留口；省略默认当前会话
- 检索工具调用不进输出信号，只在循环内部回灌；格式错误重试一次再丢弃

## 四、回复策略（Policy）

- 私聊必回；群聊 @我 必回（逐条、登记防重）；主人说话必回
- 群聊普通消息自由决策，[SILENT] 合法
- 必回场景模型沉默 → 强制重试一次，仍沉默用兜底话术

## 五、任务分发（与工具层的对话）

用户需求超出"回一句话"（找图发图/查资料/整理文件/多步操作）时，
组装 `TaskBrief(goal, context, tried, deliver + 输出约定句)` 交工具层，
异步拿 `TaskResult` 后人格化回复。ActionResult 失败带 escalation_hint 时
自动升级为任务。

## 六、Provider 抽象

- LLM 调用统一接口，模型可热替换（k3 / DeepSeek / 本地端点）
- 模型怪癖集中收口（如 k3 省略 temperature）

## 七、明确不做

- 不感知平台（不知道消息来自安卓还是桌面端）
- 不碰坐标/像素/控件，不驱动 UI 导航
- 不自己实现工具框架（那是工具层——现成 CLI 的事）
- 不直接写消息日志（读写在交互层，本层经接口访问）
